---
interface Props {
    content: string;
    url: string;
}

const { content, url } = Astro.props;
---

<a href={url} class="text-base sm:text-lg xl:text-xl bg-accent px-5 py-[10px] sm:py-3 rounded-xl">
    {content}
</a>
